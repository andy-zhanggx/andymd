{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Cross-platform capability (desktop + iOS).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
