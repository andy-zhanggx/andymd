{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only permissions: window dragging, in-app updater, and process control. These plugins/permissions don't exist on iOS, so this capability is scoped to desktop platforms.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": [
    "core:window:allow-start-dragging",
    "updater:default",
    "process:default",
    "process:allow-restart"
  ]
}
