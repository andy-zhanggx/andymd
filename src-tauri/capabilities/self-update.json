{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "self-update",
  "description": "Bundled updater + relaunch. Excluded from Mac App Store builds, where the updater plugin is not compiled in and App Review forbids a private update channel.",
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:default",
    "process:allow-restart"
  ]
}
